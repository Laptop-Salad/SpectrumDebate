header 
    div(class="logo") 
        img(src="../../assets/logo.svg")
        a(href="") = "Spectrum Debate"
    nav(class="shortcuts")
        ul
            li(class="link")
                a(href="home") = "Home"
            li(class="link")
                a(href="dashboard") = "Dashboard"
    nav
        ul
            li
                a(href="/dashboard") = "{$username}"
            li
                a(href="/logout" id="navLogout" class="logout") = "Logout"
